name: Run-Pub-Sub-Test
run-name: ${{ github.actor }} running pub_sub_test using GitHub Actions 🚀
on: [push]
jobs:
  Run-Pub-Sub-Test:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    container:
      image: ros:noetic
      volumes:
        - ${{ github.workspace }}:/root/ros-test
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub"
      - run: echo "🔎 Branch ${{ github.ref }} Repo ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
        
      - name: Build catkin workspace and project
        run: |
          cd /root/ros-test
          chmod +x ./remake_ws.bash
          ./remake_ws.bash
        shell: bash          
        
      - name: List and verify important files are in the noetic container.
        run: |
          echo "Contents of /:"
          ls -al /root/ros-test
          echo "Contents of my_ws:"
          ls -al /root/ros-test/my_ws
          echo "Contents of my_ws/devel/:"
          chmod +x /root/ros-test/my_ws/devel/*
          ls -al /root/ros-test/my_ws/devel
          echo "Contents of my_ws/src/:"
          ls -al /root/ros-test/my_ws/src
          echo "Contents of my_ws/src/my_pkg:"
          ls -al /root/ros-test/my_ws/src/my_pkg
          echo "Contents of my_ws/src/my_pkg/scripts:"
          chmod +x /root/ros-test/my_ws/src/my_pkg/scripts/*.py
          ls -al /root/ros-test/my_ws/src/my_pkg/scripts
          echo "Contents of my_ws/src/my_pkg/test:"
          chmod +x /root/ros-test/my_ws/src/my_pkg/test/*.py
          ls -al /root/ros-test/my_ws/src/my_pkg/test
        shell: bash          

      - name: Run simple python script to check python, docker, and ubuntu versions.
        run: |
          ./list_sw_version_info.py
        shell: bash          

      - name: Run pub_sub_test.
        run: |
          source /opt/ros/noetic/setup.bash
          source /root/ros-test/my_ws/devel/setup.bash
          rostest my_pkg pub_sub_test.test
          catkin_test_results /github/home/.ros/test_results
        shell: bash

      - name: Run unit tests.
        run: |
          source /opt/ros/noetic/setup.bash
          source /root/ros-test/my_ws/devel/setup.bash       
          /root/ros-test/my_ws/src/my_pkg/test/test_listener.py
        shell: bash          
          
      - run: echo "🍏 This job's status is ${{ job.status }}."